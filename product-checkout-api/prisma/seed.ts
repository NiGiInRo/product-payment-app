async function main() {
  console.log(
    'Seed placeholder: Slice 1 solo prepara la infraestructura. El producto semilla se agregara en el Slice 2.',
  );
}

main().catch((error: unknown) => {
  console.error(error);
  process.exit(1);
});
